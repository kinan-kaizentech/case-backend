{
  "name": "recipe-api",
  "version": "1.0.0",
  "description": "Turkish Recipe API",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "postbuild": "cp src/data/*.json dist/data/ && cp swagger.yaml dist/",
    "start": "node dist/index.js"
  },
  "author": "Kaizen FE Team",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.2.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
